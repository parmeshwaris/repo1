pipeline{
    agent none
    stages{
        stage('checkout scm'){
         agent{
            label 'slave1'
             }
       steps{
            echo "this stage runs on node ($env.label)"
        }

        stage('Build'){
              agent{
            label 'slave2'
               }
           steps{
            echo "this stage runs on node ($env.label)"
        }
    }
}
